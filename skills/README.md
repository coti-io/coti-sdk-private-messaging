# COTI Private Messaging Skill

This repository includes an Open Plugins / Cursor-compatible skill at:

```text
skills/coti-private-messaging/SKILL.md
```

Open Plugins scanners look for `skills/*/SKILL.md` at the repository root. Keep this folder in the SDK repo so the skill is detected when the repository is imported as a plugin source.
